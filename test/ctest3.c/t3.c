/*
Write a C program to find the median of a merged sorted Array by using pointer and function.
*/

#include<stdio.h>
#include<conio.h>

int main()
{
    int a[10], b[10], c[20], n, m, i,j, k, temp, flag = 0;
    
     printf("Enter the number of elements in array a: ");
     scanf("%d", &n);

            
}